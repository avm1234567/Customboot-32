cmake_minimum_required(VERSION 3.5)

# Set project name and target
project(esp32_ota_spiffs)

# Initialize the ESP-IDF build system
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(esp32_ota_spiffs)

